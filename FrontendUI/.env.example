# PUBLIC_INTERFACE
# API base URL for the backend (e.g., http://localhost:8000/api/v1)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# PUBLIC_INTERFACE
# JWT token placeholder for initial scaffolding if backend cookie auth is not available
VITE_DEV_JWT=REPLACE_WITH_DEV_JWT
