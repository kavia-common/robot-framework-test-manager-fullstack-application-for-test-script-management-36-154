# Vite environment variables must be prefixed with VITE_
# API base URL for the backend. Change this per environment.
# Local development default points to FastAPI running at http://localhost:8000 with /api/v1 prefix.
VITE_API_BASE_URL=http://localhost:8000/api/v1
